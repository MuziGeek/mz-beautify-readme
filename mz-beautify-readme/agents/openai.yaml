interface:
  display_name: "MZ Beautify README"
  short_description: "Localized README design with optional Muzi overlay"
  default_prompt: "Use $mz-beautify-readme to apply the pinned upstream README core, generate continuous-scene localized assets for every README language, add Muzi only when routing permits, and do not publish."
