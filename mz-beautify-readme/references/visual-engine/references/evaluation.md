# Evaluation

Use `pass`, `warn`, `fail`, or `not_applicable` for every check. Attach evidence to every result; do not invent percentage style scores.

A downstream asset is eligible for `READY_FOR_REVIEW` only when there is no hard failure, this Engine's Core/Profile/Preset checks pass, and the target Skill's own mechanical and visual checks have completed.

The Engine does not replace `mz.icon-batch/1`, `creator.mz-crayon-review/1`, or a target Skill's own review contract.
